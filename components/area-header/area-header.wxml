<!--components/area-header/area-header.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view 
  class="more"
  bindtap="onMoreTap"
  wx:if="{{hasMore}}">
    <text class="txt">更多</text>
    <van-icon name="arrow"/>
  </view>
</view>
